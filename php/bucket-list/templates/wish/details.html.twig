{% extends 'base.html.twig' %}

{% block title %} {{ wish.title }} {% endblock %}

{% block main %}
    <h1>{{ wish.title }}</h1>
    <p>{{ wish.description }}</p>
    <p>{{ wish.author }}</p>
{% endblock %}