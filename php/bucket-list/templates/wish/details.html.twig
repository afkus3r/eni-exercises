{% extends 'base.html.twig' %}

{% block title %} {{ wish.title }} {% endblock %}

{% block main %}
    <h2>{{ wish.title }}</h2>
    <p>{{ wish.description }}</p>
    <p>{{ wish.author }}</p>
{% endblock %}